<div class="vue-controls-incrementer" nid="{{ nid }}" score="{{ score }}" field="{{ field }}" type="{{ type }}">
  <span>${ score }</span>
  <div>
    <span @click="decreaseScore">-</span>
    <span @click="increaseScore">+</span>
  </div>
</div>
